export enum ShopState {
  Product = 'productState',
  Cart = 'cartState',
}
export enum ShopAction{
  AddProduct = '[Shop] Add Product',
  RemoveProduct = '[Shop] Remove Product',
  RemoveAllProducts = '[Shop] Remove All Products',
}
